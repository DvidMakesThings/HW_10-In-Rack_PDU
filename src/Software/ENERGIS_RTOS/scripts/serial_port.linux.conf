# Linux serial config (key=value)
# Update PORT if your device appears as another name (e.g. /dev/ttyUSB0)
PORT=/dev/ttyACM0
BAUD=115200

# Firmware and tools relative to this workspace
# This matches the CMake output name set in CMakeLists.txt
FIRMWARE=../build/ENERGIS_firmware_v1.0.0.uf2
PY_UPLOADER=./rp2040_upload.py
LOG_DIR=../docs/Logs
